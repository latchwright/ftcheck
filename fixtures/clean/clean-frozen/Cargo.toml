[package]
name = "clean-frozen"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "clean_frozen"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
