[package]
name = "clean-buffer-protocol"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "clean_buffer_protocol"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
