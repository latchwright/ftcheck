[package]
name = "ft001-static-mut"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "ft001_static_mut"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
