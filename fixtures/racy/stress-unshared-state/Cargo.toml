[package]
name = "stress-unshared-state"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "stress_unshared_state"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
